## Agent skills

### Issue tracker

Issues live in GitHub Issues for `AgustinLarzabal/kona`, accessed via the `gh` CLI. See `docs/agents/issue-tracker.md`.

### Triage labels

Default canonical vocabulary (`needs-triage`, `needs-info`, `ready-for-agent`, `ready-for-human`, `wontfix`). See `docs/agents/triage-labels.md`.

### Domain docs

Multi-context layout — `CONTEXT-MAP.md` at the repo root pointing at per-context `CONTEXT.md` files under `apps/*` and `packages/*`. See `docs/agents/domain.md`.
